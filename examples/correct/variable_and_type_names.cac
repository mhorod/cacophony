#variable names should start with lowercase letter

let i = 7;
let longerVariableName = true;

#we allow underscore for variables

let _internal_use_variable_ = 1;

#type names should start with uppercase letter

let count: Int = 0;
let check: Bool = false;

#functions are variables too

let function = [x: Int] -> Int => x;