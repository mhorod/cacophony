let empty = []->Unit=>();

let f = [a: Int] -> Unit => (
    let x = $a;
    empty[];
    if (@x % 2) == 1 then (@x = @x + 1;);
);

let i : Int = 0;
while (i < 10000000) do (
    f[i];
    i = i + 1;
);
